<div class="container px-4 py-5 my-5">
    <h1 class="display-5 fw-bold"><%= @page.title %></h1>
    <h2 class="display-6 fw-light"><%= @page.description %></h2>
    <div class="container py-5">
        <%= sanitize @page.body %>
    </div>
</div>